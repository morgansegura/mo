---
templateKey: projects
title: Test Project 1
date: 2020-12-13
published: true
cover: ../../media/large-18.jpg
---

## Yes! Some code!

<div className=" lg:grid grid-cols-2 gap-4">
    <div className="lg:w-1/3 bg-black bg-opacity-25 p-8 rounded-lg">
        <div>Project Name</div>
        <div>Title</div>
        <div>Title</div>
        <div>Title</div>
    </div>
    <div className="lg:w-2/3 p-8">This is where the images will go</div>
</div>

Here is the `Dump` component!

```jsx
import React from "react"

const Dump = props => (
    <div
        style={{
            fontSize: 20,
            border: "1px solid #efefef",
            padding: 10,
            background: "white",
        }}
    >
        {Object.entries(props).map(([key, val]) => (
            <pre key={key}>
                <strong style={{ color: "white", background: "red" }}>
                    {key} ?
                </strong>
                {JSON.stringify(val, "", " ")}
            </pre>
        ))}
    </div>
)

export default Dump
```
